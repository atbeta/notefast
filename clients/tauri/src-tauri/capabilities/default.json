{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "NoteFast 主窗口默认能力（invoke engine_start 为自定义命令，不在 ACL 内）",
  "windows": ["main"],
  "permissions": ["core:default"]
}
