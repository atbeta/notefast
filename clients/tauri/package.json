{
  "name": "@notefast/tauri-client",
  "version": "0.58.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "prepare:engine": "bun run scripts/prepare-resources.ts",
    "build:full": "bun run prepare:engine && tauri build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
