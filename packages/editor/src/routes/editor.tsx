/** 核心路由：编辑器 + 预览 + 工具栏（M2 填充 shared 的 CodeMirrorEditor / ChatMarkdown）。 */
export default function EditorView() {
  return (
    <main className="flex min-h-screen flex-col p-8">
      <p className="text-sm text-muted-foreground">编辑器骨架（M2 接入 shared 编辑器与预览）。</p>
    </main>
  )
}
